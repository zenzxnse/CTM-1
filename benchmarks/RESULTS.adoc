= Benchmark results

Status:: No current release measurement recorded
Date:: 2026-09-11

The previous smoke measurements used a retired service boundary. They are retained
in the dated raw JSON files as historical evidence and must not be used as results
for the active external-telemetry path.

Run the procedure in link:README.adoc[the methodology] after the final source edit.
Save the raw report outside the source tree or under an ignored build directory,
then generate plots with `benchmarks/plot.py`. Do not fill this record with target
values or measurements from another build.
